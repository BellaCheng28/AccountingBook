<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul>
      <% accountingBooks.forEach((book)=>{%>
       <li>
        <a href="/accountingbooks/<%=book._id%><%book.name%>"></a>
        <% if (book.owner.toString()===user._id.toString()){%>
          <span>my accountingbook</span>
        <%}else{%>
          <span>Shared accountingbook</span>
        <%}%>
       </li>

      <%})%>
    </ul>

</body>
</html>